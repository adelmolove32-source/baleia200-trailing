services:
  - type: web
    name: baleia200-trailing
    runtime: python
    repo: https://github.com/SEU_USUARIO/SEU_REPO
    plan: free
    region: ohio
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT --workers 1 --threads 2 --timeout 0

